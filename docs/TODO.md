# Implementation TODO

Work through these in order. Each item is a self-contained module. After completing each item,
run `make lint-fix && make lint && make test` and fix any issues before moving on.

Reference `docs/DESIGN.md` for the full architecture, config schema, SQLite schema, and
business logic details. All configuration must be read from `from config import settings`.

---
